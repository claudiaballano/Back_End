<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head'); %>
</head>

<body>
    <%- include('./partials/navigation'); %>
    <header>
        <%- include('./partials/header'); %>
    </header>
    <form action="/gallery/add-photo" method="POST">
        <div class="margin">
            <h4>Añadir imagen nueva</h1>
                <% if (urlUsed) { %>
                <p class="url">La URL: <%= url %> ya existe</p>
                <% } %>
                <% if (incorrectUrl) { %>
                <p class="url">Introduce una URL válida. Solo se permiten fotografias de Picsum</p>
                <% } %>
                <label for="titulo">Titulo:</label>
                <input type="text" id="titulo" name="titulo" maxlength="10" pattern="^[a-zA-Z0-9_]*$" required>
                <label for="url">Url:</label>
                <input type="url" id="url" name="url" required>
                <label for="fecha_creacion">Fecha:</label>
                <input type="date" id="fecha_creacion" name="fecha_creacion" required>
                <input type="submit" value="submit">
        </div>
    </form>
</body>
<style>
    body {
        margin: 4rem
    }
    form {
        border: #ab5dda solid 1px;
        border-radius: 10px;
        max-width: 50%;
        margin:auto
        
    }

    .margin {
        margin: 2rem;
    }
    .url{
        color: red;
        background-color: rgb(250, 192, 192);
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        font-weight: 600;
        font-size: 1.2rem;
        padding: 1.3rem;
        border-radius: 10px;
    }
</style>

</html>